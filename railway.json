{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "installCommand": "cd frontend && npm install --legacy-peer-deps && cd ../backend && npm install --legacy-peer-deps",
    "buildCommand": "cd frontend && npm run build && cp -r dist ../backend/frontend-dist"
  },
  "deploy": {
    "startCommand": "cd backend && npx tsx src/index.ts",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
