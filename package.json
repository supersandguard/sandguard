{
  "name": "sandguard",
  "version": "0.2.0",
  "private": true,
  "description": "SandGuard — Transaction Firewall for Safe Multisig",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npx tsx src/index.ts",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npx esbuild src/index.ts --bundle --platform=node --outfile=dist/server.cjs --external:ethers --format=cjs",
    "build": "npm run build:frontend && npm run build:backend",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  }
}
