# DEPRECATED: Frontend is now served from Railway alongside the backend.
# Netlify deployment has been removed. Railway auto-deploys from main branch.
#
# To re-enable, uncomment the workflow below and set NETLIFY_AUTH_TOKEN + NETLIFY_SITE_ID secrets.

name: "[DISABLED] Build & Deploy to Netlify"
on:
  workflow_dispatch:  # Manual trigger only, no longer auto-deploys

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Frontend is deployed on Railway."
